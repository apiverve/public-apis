openapi: 3.0.0
info:
  title: Disposable Phone Number Checker
  version: '1'
  description: >-
    Disposable Phone Number Checker is an API that checks whether a given phone
    number is a known disposable or temporary number. Useful for fraud
    prevention, user verification, and security.
  contact:
    name: APIVerve
    url: https://apiverve.com
    email: hello@apiverve.com
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
servers:
  - url: https://api.apiverve.com
    description: Production server
security:
  - ApiKeyAuth: []
paths:
  /v1/disposablephone:
    get:
      summary: Disposable Phone Number Checker
      description: >-
        Disposable Phone Number Checker is an API that checks whether a given
        phone number is a known disposable or temporary number. Useful for fraud
        prevention, user verification, and security.
      operationId: disposablephone
      tags:
        - null
      parameters: []
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
                  data:
                    type: object
        '400':
          description: Bad Request - Invalid parameters
        '401':
          description: Unauthorized - Invalid or missing API key
        '429':
          description: Too Many Requests - Rate limit exceeded
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: x-api-key
  schemas: {}
